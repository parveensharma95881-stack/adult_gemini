name: balaji
description: "Balaji Maharaj App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  battery_plus: ^6.0.0
  battery_info: ^1.1.0
  flutter_tts: ^4.1.0
  flutter_inappwebview: ^6.0.0
  intl: ^0.19.0
  workmanager: ^0.5.2 # बैकग्राउंड में चलाने के लिए

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/html/
    - assets/database/panchang.db
