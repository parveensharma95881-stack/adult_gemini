name: Build APK
on:
  push:
    branches: [ main ]
  workflow_dispatch:

# 🚀 Node 20 वाली पीली चेतावनी को कुचलने के लिए
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          # यहाँ 'stable' रखने से तुझे Dart 3.10.x के आस-पास का वर्ज़न मिल जाएगा
          channel: 'stable'
          cache: true

      - name: Build APK
        run: |
          flutter clean
          flutter pub get
          flutter build apk --release --target-platform android-arm64 --no-shrink

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Balaji-App-Final-Success
          path: build/app/outputs/flutter-apk/app-release.apk
